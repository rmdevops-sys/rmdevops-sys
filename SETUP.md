# Final GitHub profile setup

Repository:
`https://github.com/rmdevops-sys/rmdevops-sys`

Replace the existing README with this README and keep the assets folder.

Files:
- `README.md`
- `assets/profile.jpg`
- `assets/hacker-banner.gif`
- `.github/workflows/contribution-snake.yml`

After pushing:
1. Open GitHub Actions.
2. Run **Generate contribution snake** manually once.
3. The action creates/updates the `output` branch.
4. Refresh the profile page.

Important:
- No CSS animation is used because GitHub sanitizes README CSS.
- The hacker animation is a real GIF, so it works in GitHub README.
- The external stats/activity graph services that caused 402 errors are removed.
- Email is `hello@rmdevops.in`.
- No GreenEgle is included.

# Premium GitHub Profile

Replace the files in `rmdevops-sys/rmdevops-sys` on `main`:

- README.md
- assets/profile.jpg
- assets/premium-hero.gif
- assets/terminal.gif

Keep your existing `.github/workflows/contribution-snake.yml`.

This version uses real GIF animation because GitHub does not reliably allow CSS/JS animation in README files. External stats services that caused 402 errors are not used.
